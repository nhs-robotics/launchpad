# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# We no longer need to auto-convert third-party libraries to use AndroidX, which slowed down the build
android.enableJetifier=false

# Packaging native libraries (OpenCV, AprilTag, etc.) during packageDebug/packageRelease is
# memory-hungry; 1GB was too tight and caused CI packaging failures.
org.gradle.jvmargs=-Xmx3072M

android.nonTransitiveRClass=false